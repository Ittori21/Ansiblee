0.Написать однострочник который раз в секунд дергает https://s3.mdst.yandex.net/ping и выводит код ответа, timestamp и время запроса

1. Раздача статичных файлов через nginx
Задание: вы должны установить nginx на виртуалку/на свой хост и написать такую конфигурацию, чтобы при открытии по ручке /files вы могли видеть все файлики 

Целевая картинка: вы можете открыть вебку nginx и увидеть какое-то количество файликов к примеру как в https://mirror.yandex.ru/debian/tools/

2. Написать свой модуль который (на выбор):
• принимает в качестве параметра порт для nginx и подставляет его в конфигурацию

Запуск на локальной машине
sudo ansible-playbook -i inventory.ini playbook.yml

Проверка
curl http://localhost:8080/files/
